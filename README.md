# elemental-isles
Legends of Elemental Isles game for my NEA


Setup the venv:

`python3 -m venv venv`

Source it:

`source venv/bin/activate`

Install pip modules:

`pip install -r requirements.txt`

Run the game:

`python filecopy/main.py`
